class LocationService {
    static calculateDistance(coords1, coords2) {
        // Logic to calculate distance between two GeoJSON points (Haversine formula)
        return 0; // returns distance in km
    }
}

module.exports = LocationService;
